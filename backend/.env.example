# Backend Volta – copiază ca .env și completează valorile
# cp .env.example .env

NODE_ENV=development
PORT=3000

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_NAME=volta_db

# JWT pentru autentificare (obligatoriu în producție – generează un secret lung și aleatoriu)
JWT_SECRET=volta-dev-secret-schimba-in-productie

# Cheie API pentru panoul Admin (opțional; dacă lipsește, rutele admin sunt deschise – doar pentru dev)
# ADMIN_API_KEY=cheie-secreta-admin

# URL bază pentru imagini (opțional; dacă lipsește se folosește localhost:PORT)
# IMAGE_BASE_URL=http://localhost:3000

# CORS (în producție setează domeniile permise, separate prin virgulă)
# CORS_ORIGIN=https://app.volta.md,https://admin.volta.md
