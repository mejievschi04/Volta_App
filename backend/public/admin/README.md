# Admin Panel Volta

Panoul admin este servit static de la `/admin`. Fișierul principal este `index.html`.

Pentru a folosi API-ul din alt domeniu, setează URL-ul API în câmpul din header (ex: `http://localhost:3000/api`).
